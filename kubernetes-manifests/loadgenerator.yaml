# Copyright 2018 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: loadgenerator
  labels:
    app: loadgenerator
spec:
  selector:
    matchLabels:
      app: loadgenerator
  replicas: 1
  template:
    metadata:
      labels:
        app: loadgenerator
      annotations:
        sidecar.istio.io/rewriteAppHTTPProbers: "true"
    spec:
      serviceAccountName: loadgenerator
      terminationGracePeriodSeconds: 5
      restartPolicy: Always
      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
      initContainers:
      - command:
        - /bin/sh
        - -exc
        - |
          MAX_RETRIES=12
          RETRY_INTERVAL=10
          for i in $(seq 1 $MAX_RETRIES); do
            echo "Attempt $i: Pinging frontend: ${FRONTEND_ADDR}..."
            STATUSCODE=$(wget --server-response http://${FRONTEND_ADDR} 2>&1 | awk '/^  HTTP/{print $2}')
            if [ $STATUSCODE -eq 200 ]; then
                echo "Frontend is reachable."
                exit 0
            fi
            echo "Error: Could not reach frontend - Status code: ${STATUSCODE}"
            sleep $RETRY_INTERVAL
          done
          echo "Failed to reach frontend after $MAX_RETRIES attempts."
          exit 1
        name: frontend-check
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          privileged: false
          readOnlyRootFilesystem: true
        image: busybox:latest
        env:
        - name: FRONTEND_ADDR
          value: "frontend:80"
      containers:
      - name: main
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          privileged: false
          readOnlyRootFilesystem: true
        image: loadgenerator
        env:
        - name: FRONTEND_ADDR
          value: "frontend:80"
        - name: USERS
          value: "10"
        - name: RATE
          value: "1"
        # Replace the image's bundled locustfile (which targets the
        # upstream Google sample product IDs and 404s against the
        # Akamai-themed catalog) with one that uses our AKMT* SKUs.
        volumeMounts:
        - name: locust
          mountPath: /loadgen/locustfile.py
          subPath: locustfile.py
          readOnly: true
        resources:
          requests:
            cpu: 300m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
      volumes:
      - name: locust
        configMap:
          name: loadgenerator-locust
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: loadgenerator-locust
  labels:
    app: loadgenerator
data:
  locustfile.py: |
    # Akamai-themed locust scenarios. Product IDs are AKMT001..AKMT029
    # and match the MongoDB-backed productcatalogservice.
    import datetime
    import random
    from locust import HttpUser, TaskSet, between
    from faker import Faker

    fake = Faker()

    products = [f"AKMT{n:03d}" for n in range(1, 30)]
    currencies = ["EUR", "USD", "JPY", "CAD", "GBP", "TRY", "KRW", "CNY"]

    def index(l):
        l.client.get("/")

    def setCurrency(l):
        l.client.post("/setCurrency", {"currency_code": random.choice(currencies)})

    def browseProduct(l):
        l.client.get("/product/" + random.choice(products))

    def viewCart(l):
        l.client.get("/cart")

    def addToCart(l):
        product = random.choice(products)
        l.client.get("/product/" + product)
        l.client.post("/cart", {
            "product_id": product,
            "quantity": random.randint(1, 10),
        })

    def empty_cart(l):
        l.client.post("/cart/empty")

    def checkout(l):
        addToCart(l)
        current_year = datetime.datetime.now().year + 1
        l.client.post("/cart/checkout", {
            "email": fake.email(),
            "street_address": fake.street_address(),
            "zip_code": fake.zipcode()[:5],
            "city": fake.city(),
            "state": fake.state_abbr(),
            "country": "United States",
            # Faker returns a Luhn-valid Visa number (no dashes), which
            # the frontend's go-playground validator accepts.
            "credit_card_number": fake.credit_card_number(card_type="visa"),
            "credit_card_expiration_month": "1",
            "credit_card_expiration_year": str(current_year),
            "credit_card_cvv": "672",
        })

    class UserBehavior(TaskSet):
        def on_start(self):
            index(self)

        tasks = {
            index: 1,
            setCurrency: 2,
            browseProduct: 10,
            addToCart: 2,
            viewCart: 3,
            checkout: 1,
        }

    class WebsiteUser(HttpUser):
        tasks = [UserBehavior]
        wait_time = between(1, 10)
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: loadgenerator
